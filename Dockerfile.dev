FROM node:20-alpine

WORKDIR /app
ENV HUSKY=0

COPY package*.json ./
RUN npm ci

EXPOSE 5173

# Always install dependencies on container start to pick up new packages
CMD ["sh", "-c", "npm ci && npm run dev -- --host 0.0.0.0 --port 5173"]
