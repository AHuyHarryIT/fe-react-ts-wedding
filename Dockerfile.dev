FROM node:20-alpine

WORKDIR /app
ENV HUSKY=0

COPY package*.json ./
RUN npm ci

EXPOSE 5173

CMD ["sh", "-c", "if [ ! -d node_modules/.bin ]; then npm ci; fi && npm run dev -- --host 0.0.0.0 --port 5173"]
