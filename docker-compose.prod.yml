services:
  staff:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: studio-hamy-staff
    restart: unless-stopped
    env_file:
      - .env.prod
    ports:
      - "5173:80"
